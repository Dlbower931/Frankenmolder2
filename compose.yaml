services:
  hello:
    build: .          # tells Compose to build using Dockerfile in this folder
    command: ["python", "main.py"]  # optional override; mirrors CMD
    volumes:
      - .:/app        # mount current dir (so edits show up if you rerun)