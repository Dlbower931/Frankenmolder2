cmake_minimum_required(VERSION 3.0.2)
project(catkin_workspace)

# Find catkin and other packages
find_package(catkin REQUIRED)

# Tell catkin to look for packages inside the 'src' subdirectory
catkin_workspace()